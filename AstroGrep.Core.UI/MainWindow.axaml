<Window
  xmlns="https://github.com/avaloniaui"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:dd="clr-namespace:AstroGrep.Core.UI;assembly=AstroGrep.Core.UI"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:sys="clr-namespace:System;assembly=netstandard"
  xmlns:collections="clr-namespace:System.Collections;assembly=System.Runtime"
  mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
  Icon="/Assets/search.ico"
  x:Class="AstroGrep.Core.UI.MainWindow"
  Title="AstroGrep Lite">
  <Design.DataContext>
    <dd:MainWindowViewModel />
  </Design.DataContext>

  <Window.Resources>
    <!--
      Have to use ArrayList as a workaround
        x:Array in XAML
          https://github.com/AvaloniaUI/Avalonia/issues/3661
    -->
    <collections:ArrayList x:Key="FileFilters">
      <sys:String>*.cs</sys:String>
      <sys:String>*.csproj</sys:String>
      <sys:String>*.json</sys:String>
      <sys:String>*.xaml</sys:String>
      <sys:String>*.sln</sys:String>
      <sys:String>*.c</sys:String>
      <sys:String>*.cpp</sys:String>
      <sys:String>*.h</sys:String>
      <sys:String>*.hpp</sys:String>
      <sys:String>*.vcproj</sys:String>
    </collections:ArrayList>
  </Window.Resources>

  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition Width="3" />
      <ColumnDefinition />
    </Grid.ColumnDefinitions>

    <DockPanel LastChildFill="True" Margin="10">
      <TextBlock DockPanel.Dock="Top" Foreground="Purple" Text="Astrogrep Lite Search" />
      <Border DockPanel.Dock="Top" Height="2" BorderBrush="Purple" BorderThickness="1" />
      <Border DockPanel.Dock="Top" Height="10" />

      <TextBlock DockPanel.Dock="Top" Text="Search path:" />
      <DockPanel DockPanel.Dock="Top" LastChildFill="True">
        <Button DockPanel.Dock="Right" Command="{Binding OnSelectFolder}">
          <Panel>
            <Image Source="/Assets/folder.png" Height="20" Width="20"></Image>
          </Panel>
        </Button>
        <TextBox Text="{Binding SelectedFolder, Mode=TwoWay}"></TextBox>
      </DockPanel>
      <Border DockPanel.Dock="Top" Height="10" />

      <TextBlock DockPanel.Dock="Top" Text="File types:" />
      <AutoCompleteBox DockPanel.Dock="Top" Items="{StaticResource FileFilters }" Text="{Binding FileType, Mode=TwoWay}" />
      <Border DockPanel.Dock="Top" Height="10" />

      <TextBlock DockPanel.Dock="Top" Text="Search text:" />
      <TextBox DockPanel.Dock="Top" Text="{Binding SearchText, Mode=TwoWay}" />
      <Border DockPanel.Dock="Top" Height="10" />

      <Button DockPanel.Dock="Top" Command="{Binding OnSearch}">Search</Button>
      <Border DockPanel.Dock="Top" Height="10" />

      <TextBlock DockPanel.Dock="Top" Foreground="Purple" Text="Search Options" />
      <Border DockPanel.Dock="Top" Height="2" BorderBrush="Purple" BorderThickness="1" />
      <Border DockPanel.Dock="Top" Height="10" />

      <CheckBox DockPanel.Dock="Top" IsChecked="True">Regular expressions</CheckBox>
      <Border DockPanel.Dock="Top" Height="10" />

      <CheckBox DockPanel.Dock="Top" IsChecked="True">Case sensitive</CheckBox>
      <Border DockPanel.Dock="Top" Height="10" />

      <CheckBox DockPanel.Dock="Top" IsChecked="True">Whole word</CheckBox>
      <Border DockPanel.Dock="Top" Height="10" />

      <CheckBox DockPanel.Dock="Top" IsChecked="True">Search subfolders</CheckBox>
      <Border DockPanel.Dock="Top" Height="10" />

      <CheckBox DockPanel.Dock="Top" IsChecked="True">Show filename only</CheckBox>
      <Border DockPanel.Dock="Top" Height="10" />

      <CheckBox DockPanel.Dock="Top" IsChecked="True">Negation</CheckBox>
      <Border DockPanel.Dock="Top" Height="10" />

      <CheckBox DockPanel.Dock="Top" IsChecked="True">Show all results after search</CheckBox>
      <Border DockPanel.Dock="Top" Height="10" />

      <DockPanel DockPanel.Dock="Top" LastChildFill="True">
        <NumericUpDown DockPanel.Dock="Left" Value="4" Minimum="1" Maximum="100" Width="120" />
        <Border DockPanel.Dock="Left" Width="10" />
        <TextBlock Text="Context lines" />
      </DockPanel>
      <Border DockPanel.Dock="Top" Height="10" />

      <Button DockPanel.Dock="Top">Exclusions...</Button>
      <Border DockPanel.Dock="Top" Height="10" />
    </DockPanel>

    <GridSplitter Grid.Column="1" Background="Purple" ResizeDirection="Columns" />

    <Grid Grid.Column="2">
      <Grid.RowDefinitions>
        <RowDefinition />
        <RowDefinition Height="3" />
        <RowDefinition />
      </Grid.RowDefinitions>

      <TextBox AcceptsReturn="True" Text="{Binding DumpMatch}"></TextBox>
      <GridSplitter Grid.Row="1" Grid.ColumnSpan="3" Background="Purple" ResizeDirection="Rows" />
      <TextBox Grid.Row="2" AcceptsReturn="True" Text="{Binding SelectedFolder, Mode=TwoWay}"></TextBox>
    </Grid>

  </Grid>
</Window>